# 项目约定

1. **每次任务完成要详细记录日志**：在 `logs/` 目录下追加一条记录，包含日期、任务描述、关键参数、产出位置、遇到的问题与解决方式。
2. **每次任务完成更新所有相关文档**：包括 `README.md`、`CLAUDE.md`、脚本顶部 docstring 等，确保用法示例与当前代码一致。
3. 要求获得真实图片，不得包括如卡通、雕塑、表情包、艺术设计、绘画等
4. 真实图片数量不局限于50个，最后过滤出来有多少就存多少
4. 真实图片数量不局限于50个，最后过滤出来有多少就存多少
