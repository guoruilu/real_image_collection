# 2026-05-12 实验列表瘦身 + 新增类数缩放实验 + ablation 概念解释

## 用户三个要求

1. 新增实验：end-to-end VQA acc 在随机子采样 10/20/30/40 类上的折线图
2. 实验太多，按 4 页篇幅瘦身：核心 vs 选做；MLLM 用 1 个就够
3. "Top-K 候选传递 / EEG 置信度阈值 fall back 到全图"没看懂，要详解

## 修改

### `docs/research_plan.md`（速览版）"Stage 3 实验设计" 整段重写

- 标题改为 "Stage 3 实验设计（按 4 页篇幅，区分核心/选做）"
- 加一句 "MLLM 用 1 个代表（如 GPT-4o）即可"
- **核心 4 项**：
  1. 效率主轴
  2. VQA 双轨
  3. **【新增】VQA acc - 类数折线图（10/20/30/40 类，每个 K 多 seed 平均 + 误差棒）**
  4. 按问题类型分类（局部属性 vs 场景上下文）
- **选做 4 项**：OD 框误差 / 跨 MLLM / Top-K ablation / 置信度 fall back / V\*-SparseVLM 对比
- 末尾新增 "概念解释"：Top-K 候选传递（举埃及猫/波斯猫例）+ 置信度 fall back 的明确定义

### `docs/research_plan_details.md` 修改

- §2.5.6 整段重写为详解版：
  - (a) Top-K 候选传递：做法 / 为什么有用 / 举例 / 代价
  - (b) EEG 置信度阈值 fall back：做法 / 为什么有用 / 代价
  - (c) VQA 问题筛选：标注为消极方案不推荐
- §2.5.8 重写：核心 4 项 + 选做 5 项 + 4 页篇幅预估
  - 核心 #3 写明新增的类数缩放实验：用 N=5 次随机子采样、画折线图、量化"减少类数的收益曲线"、暗示 BCI 小类数应用已实用、给未来 EEG 改进收益空间画上界

### `docs/research_plan.md` "下一步" 段落

- 删除原 "Stage 2 接受 top-K 候选" 的硬性要求（已降级为选做 ablation）
- 第 3 步明确为 "定 1 个 MLLM + 跑核心 1–4"
- 第 4 步加选做项

## 备注

VQA acc-类数折线图是新增的核心实验，需要 Stage 1 在不同类数下重新训练或子集筛选，工作量需评估。如果时间紧，可以只在 10/20/40 三点跑（去掉 30）。
